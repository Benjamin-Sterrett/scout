# Verification Evidence

**Date:** 2026-04-02
**Ticket:** PRJ-370
**Wave:** 3 — Pipeline orchestrator + CLI

## Test Results

- **pytest:** 235 passed, 0 failed (1.01s) — PASS
  - 203 existing tests (models, gates, scoring, intake) — all passing
  - 19 new pipeline tests
  - 13 new CLI tests
- **mypy --strict (full repo):** Success: no issues found in 17 source files — PASS

## What Was Tested

### Pipeline (test_pipeline.py — 19 tests)
- Full pipeline run with all 10 fixtures
- Stats populated correctly (total_parsed, passed_gates, rejected, needs_review sum to total)
- has_pre_scores flag correct for both modes
- Confidence distribution counts
- Ranked listings sorted by total_score descending
- top_n slicing works
- MVP listing correctly rejected
- Equity listing correctly rejected
- Free test work listing correctly rejected
- Rejected listings never appear in ranked bucket
- LOW confidence listing routed to needs_review or rejected
- Borderline budget ($140 MAYBE zone) goes to needs_review
- MAYBE listing has gate_results populated
- Empty input returns empty result with zero stats
- Pre-scores mode sets has_pre_scores=True
- Pre-scores produce nonzero total_score
- High pre-scores produce IMMEDIATE/PRIORITY SLA

### CLI (test_cli.py — 13 tests)
- --file flag reads and processes listings
- stdin input works
- --json flag produces valid JSON with correct structure
- JSON has stats fields (total_parsed, passed_gates, confidence_distribution)
- --top flag limits output
- --platform flag accepted
- Empty input exits nonzero
- Human output contains REJECTED section
- Human output contains Pipeline footer
- No-LLM-scores note appears when pre_scores not provided
- normalize subcommand prints PRJ-371 placeholder
- No subcommand shows help and exits nonzero
- JSON entries have required fields (title, platform, total_score, gate_results)

## Build Verification

- `python -m pip install -e ".[dev]"` — success
- `python -m scout --help` — shows score and normalize subcommands
- `python -m scout score --file <sample>` — produces correct human output
- `python -m scout score --file <sample> --json` — produces valid JSON
- `python -m scout normalize 1` — prints placeholder message
